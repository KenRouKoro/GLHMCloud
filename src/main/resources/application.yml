spring:
  application:
    name: GLHMCloud
  datasource:
    url: jdbc:h2:file:./glhmcloud/database/db
    driver-class-name: org.h2.Driver
    username: admin
    password: 123456
  jpa:
    database: h2
    hibernate:
      ddl-auto: update
    show-sql: false
  h2:
    console:
      path: /h2admin
      enabled: true

server:
  port: 8081

logging:
  config: classpath:logback-spring.xml

mqtt:
  client:
    ip: wh.korostudio.cn
    port: 1883
    name: GLHMCloud-GL
    client-id: GLHMCloud-GL
    username: glhmcloud
    password: glhm2025


############## Sa-Token ?? (??: https://sa-token.cc) ##############
sa-token:
  # token ??????? cookie ???
  token-name: glhm-auth
  # token ????????? ??30??-1 ??????
  timeout: 2592000
  # token ??????????????? token ???????????????????-1 ??????????
  active-timeout: -1
  # ?????????????? ?? true ???????, ? false ??????????
  is-concurrent: true
  # ????????????????? token ?? true ????????? token, ? false ????????? token?
  is-share: false
  # token ?????????uuid?simple-uuid?random-32?random-64?random-128?tik?
  token-style: uuid
  # ????????
  is-log: true